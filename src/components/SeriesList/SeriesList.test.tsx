import { render, screen, act, RenderResult } from '@testing-library/react';
import { BrowserRouter } from 'react-router-dom';
import { SeriesList } from '../';

describe('Test SeriesList Component', () => {
  it('test #1: should load images of the series', async () => {
    expect(true).toBe(true);
  });
});

// screen.getByRole('img', {  name: /juego de tronos/i})
